<fieldset>
  <h3>Desired Industries<span class="users_child_index"><%= f.index + 1 %><span></h3>
  <%= f.label :industry %>
  <% h = (1..Industry.count).to_a.map { |n| [Industry.find(n).name.to_sym,n] }.to_h %>
  <% h[:Blank] = 0 %>
  <%= f.select :industry_id, h.keys %>

  <%= f.hidden_field :user_id, :value => params[:id] %>
  <%= f.hidden_field :_destroy %>
  <%= link_to "削除", '#', class: "remove_fields" %>
</fieldset>
